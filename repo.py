import game

def pick_a_gun():

    gun = {
        'Loader': {
            'fire_range': 1,
            'bullet_capacity': 5
        }, 
        'Focus': {
            'fire_range': 2,
            'bullet_capacity': 4
        },
        'Close': {
            'fire_range': 3,
            'bullet_capacity': 3
        },
        'Sniper': {
            'fire_range': 4,
            'bullet_capacity': 2
        }
    }

    power = gun.fire_range * gun.bullet_capacity/1.5 + total

    for gun g = input('pick a gun: ')
    if g == gun.name:
            challenge_1 = input('you have a Bird that is running 5 feet ahead. which gun would you pick to shoot it: ').capitalize()
        if challenge_1 == 'Y':
            bird = 5 feet
    game_1 = input('which gun would you use to shoot the bird: ')
    gun_list = 

        if power > bird:
            print('you win....move to next round')
        game.Gun(name, fire_range, bullet_capacity)